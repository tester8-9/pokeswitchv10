# Mod that makes symbol encounters (visible overworld spawns) pull from the hidden encounter spawn table
[underworld]
active = false

# Mod that makes shiny pokemon visible in the overworld
[overworld_shiny]
active = false

# some sounds:
# "Play_Ba_sys_rare_effect" (battle shiny sound)
# "Play_Audience_cheer_riser_rnd"
# "Play_UI_netbattle_rank_up"
# "Play_UI_Lcircuit_Goal"
# "Play_Camp_Cooking_Explosion"
# set to "" for no sound
sound = "Play_Ba_sys_rare_effect"
# some particles:
# "bin/field/effect/particle/particle/ef_poke_symbol_aura.ptcl" (brilliant aura)
# "bin/field/effect/particle/ef_cyc_aura/ef_cyc_aura_rare_bk.ptcl" (squares from the black bike)
# "bin/field/effect/particle/ef_cyc_aura/ef_cyc_aura_rare_wh.ptcl" (stars from the white bike)
# set to "" for no particle
shiny_ptcl = "bin/field/effect/particle/ef_cyc_aura/ef_cyc_aura_rare_bk.ptcl"
# whether or not brilliant spawns show their aura
show_aura_for_brilliants = true
# whether or not to load the battle sound bank in the overworld (needed for battle shiny sound)
include_battle_sounds = true
# whether or not to play the shiny sound when the following pokemon respawns
play_sound_for_following = false
# whether or not to show the shiny particle for the following pokemon
show_ptcl_for_following = true
# whether or not to show a message box on screen when a shiny spawns
show_message_box = false
# 0 for regular shiny rate, [1,100] for modified rate
boosted_percentage = 70

# Mod that actively randomizes every new spawn
[randomizer]
active = false

# Mod that lets you tweak the camera speed, pitch range, and field of view
[camera_tweaks]
active = false

# camera speed in degrees per time unit
adjustment_speed = 3.75
# min pitch in degrees
min_pitch = -71.6197244
# max pitch in degrees
max_pitch = 14.3239449
# base camera distance
min_distance = 450.0
# zoomed out camera distance
max_distance = 1000.0

# Mod that removes the level cap-based shiny lock & catch lock
[uncap_level]
active = false

# remove all badge-based level caps (if false, shiny uncatchables are possible)
fully = true

# Mod that allows detaching the camera from the player and moving it on its own
[freecam]
active = false
disable_terrain_culling = true
# forces the extended camera to be active by default
always_use_extended_camera = false

# Mod that overwrites the maximum symbol encounter spawn count for spawners in glimwood tangle
[glimwood_overworld]
active = false

# maximum symbol count per spawner
maximum_spawns = 1

# Mod that allows you to pilot your following pokemon
[synchro_mode]
active = false

# Mod that allows following pokemon in the galar mainland wild area
[extended_following]
active = false

# Mod that tweaks fishing mechanics
[fishing_tweaks]
active = false

# whether or not to retain fishing chains after brilliant pokemon spawn
keep_chain_after_brilliant = true

# Mod allows cycling dex animations
[dex_animations]
active = false

[ai_bridge]
active = false
install_candidate_hooks = false

output_readback_post_offset = 8211420
candidate_list_post_offset = 9342476
candidate_score_post_offset = 9342576
final_selection_post_offset = 9342816

hook_output_readback_post = false
hook_candidate_list_post = false
hook_candidate_score_post = true
hook_final_selection_post = false

log_hits = true
max_log_hits = 64
candidate_dump_count = 8

force_output_score = 0
force_pokechange_enable = -1

force_final_selection = false
force_final_action_id = 0
force_final_score = 1000000

force_candidate_entry = false
force_candidate_slot = 0
force_candidate_action_id = 0
force_candidate_score = 1000000
force_candidate_enable = true

# Legacy proof-of-life force. Leave off for gameplay.
force_existing_candidates = false
force_existing_action_min = 2
force_existing_action_max = 3
force_existing_score = 1000000
force_existing_enable = true

# V7 conservative / VGC-style policy.
# 0 = off
# 1 = enable existing switch candidates, keep native score
# 2 = enable and raise score only if below switch_score
# 3 = v5-style force: enable and set score every time
# 4 = conservative mode: require full candidate pair, enable at most one candidate, native score range check
switch_policy_mode = 0
switch_min_hit = 1
switch_score = 125
switch_max_forces_per_battle = 2
switch_max_forces_per_action = 1
switch_cooldown_hits = 12
switch_action2_score = 0
switch_action3_score = 0

# V7 extra gates. These are what make it less spammy than V6.
switch_max_candidates_per_hit = 1
switch_require_candidate_count = 2
switch_min_native_score = 100
switch_max_native_score = 100
switch_preferred_action = 0
switch_alternate_actions = true
switch_native_score_only = false
switch_disable_after_force = true


# V10 move-score summary gate. This is the first attempt at VGC-ish switching:
# only enable a switch candidate if native move scoring says the current move options are not good.
switch_gate_use_move_summary = true
switch_gate_best_move_max = 0
switch_gate_require_negative_record = true
switch_gate_negative_record_threshold = -20
switch_gate_min_move_records = 8

# V8 score survey options. These are read-only diagnostics used to find move scores
# and compare them to switch candidate score=100.
score_survey_mode = false
score_survey_dump_state = false
score_survey_dump_readback_x0 = false
score_survey_dump_readback_out = false
score_survey_words = 32
